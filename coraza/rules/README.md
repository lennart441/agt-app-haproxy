# Coraza/OWASP CRS rules

Place OWASP Coreruleset here so that `coraza-spoa` can load them.

Example:

```bash
git clone https://github.com/coreruleset/coreruleset.git .
```

Required at least:
- `crs-setup.conf.example` (as referenced in conf/coraza-spoa.yaml)
- `rules/*.conf`
